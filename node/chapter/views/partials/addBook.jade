h2 Add a new book
labe(for='title') Title: 
input#bookTitle(ng-model='form.title', name='title')
p Body:
textarea#bookChapters(ng-model='form.chapters', cols='50', rows='15')
p
	button#btnAddNewBook(ng-click='submitBook()') Submit Book