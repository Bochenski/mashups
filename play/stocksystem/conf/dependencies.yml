# Application dependencies

require:
    - play
    - play -> scala 0.9.1
    - com.osinka -> mongo-scala-driver 0.8.12-SNAPSHOT
    
repositories:
    - MavenLocal:
        type: iBiblio
        root: "file:${user.home}/.m2/repository"
        contains:
            - com.osinka -> * 
